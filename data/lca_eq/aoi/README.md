# LCA-EQ AOI Metadata

This directory stores event-level AOI metadata used to identify the image
collection regions for the paper scene split. Full AOI geometries should be
provided with the Zenodo dataset record when redistribution rights permit.

`event_aoi_metadata.csv` keeps one row per earthquake event and matches the
`event_id` values used by `events.csv` and the split manifests.
