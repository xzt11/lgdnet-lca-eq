# AOI footprints

Place public AOI footprint files here, for example GeoJSON or GeoPackage files. These footprints identify the spatial extent used for annotation and patch extraction. They do not include the original third-party image pixels.
